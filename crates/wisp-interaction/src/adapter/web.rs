//! web-sys adapter (WI.7 / AUT-310) — placeholder. Body lands in
//! the next commit; this stub exists so the `cfg`-gated `mod web`
//! reference in `adapter/mod.rs` resolves cleanly under
//! `cargo fmt` and `cargo doc`.
