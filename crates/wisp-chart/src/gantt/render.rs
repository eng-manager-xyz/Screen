//! Scene-graph emission. Lands in the M-CHART.0 render chunk —
//! `Gantt::render(&Theme) -> wisp::scene::Node` builds the
//! subtree caller-side code drops into a `wisp::Stage`.
//!
//! Today the module is a placeholder so the crate compiles.
