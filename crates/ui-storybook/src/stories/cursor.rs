//! Cursor Studio stories — filled in across UI-20 (`CursorStudioShell`
//! + style picker) / UI-21 (`CursorPreviewCanvas`).

use super::Story;

/// All cursor-surface stories. Empty until UI-20 lands.
#[must_use]
pub fn stories() -> Vec<Story> {
    Vec::new()
}
