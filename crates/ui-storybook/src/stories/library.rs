//! Library stories — filled in across UI-14 (`LibrarySidebar`) / UI-15
//! (`RecordingCard` + `LibraryGrid`).

use super::Story;

/// All library-surface stories. Empty until UI-14 lands.
#[must_use]
pub fn stories() -> Vec<Story> {
    Vec::new()
}
