# Line chart

Line mark — pending AUT-189. This chapter is the placeholder
shell so the SUMMARY.md links resolve. Real prose lands when
the mark ships.

```admonish info
A line chart connects ordered `(X, Y)` rows with a stroked
polyline. Multi-series support comes via the `Color`
encoding: each distinct category becomes its own line.
```
