# Legend

Legend renderer — pending AUT-184. This chapter is the
placeholder shell so the SUMMARY.md links resolve. Real prose
lands when the renderer ships.

```admonish info
A legend visually maps the `Color` encoding's categories to
their palette swatches. The Plot facade will auto-emit a
legend when at least one encoding has `Channel::Color`.
```
